
	
	/*LinkedList<URL*> pageQueue;
		
	//create first URL object from startURL
	char* startURLString = argv[1];
	URL* startURL = new URL(startURLString);
	
	//cout << (startURL->getURL()) << endl;
	//cout << *(startURL) << endl;
	
	pageQueue.Insert(startURL, NULL);*/
	
	/*PageQueue pageQueue;
	
	char* startURLString = argv[1];
	URL* startURL = new URL(startURLString);
	
	pageQueue.enqueue(startURL);
	
	cout << pageQueue.getSize() << endl;
	do {
		URL s = pageQueue.dequeue();						//makes a copy, so URL pointer in list can be deleted as well as its contents
		cout << s.getURL() << endl;
	}
	while (!pageQueue.isEmpty());
	
	cout << "I'm here!" << endl;*/
	
	
	
	/*cout << startURL.getURL() << endl;
	pageQueue.enqueue(startURL);
	
	cout << "SIZE: " << pageQueue.getSize() << endl;
	pageQueue.printContents();
	
	URL s = pageQueue.dequeue();
	cout << s.getURL() << endl;
	
	cout << "SIZE: " << pageQueue.getSize() << endl;
	pageQueue.printContents();*/
