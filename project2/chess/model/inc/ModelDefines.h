#ifndef MODEL_DEFINES_H
#define MODEL_DEFINES_H

//an enumeration of piece types

enum PieceType {KING, QUEEN, ROOK, BISHOP, KNIGHT, PAWN};

//enumeration of two colors

enum Color {WHITE, BLACK};

//direction
enum Direction {UP, DOWN, NA};

//click
enum Click {FIRST, SECOND};

#endif
